// Deployment composition root: register replacement plugins here before React mounts.
// The built-in adapters are registered by registry.js; this file is intentionally
// empty until a customer-specific implementation needs to replace one of them.
export function configurePlugins() {
  // Example:
  // registerPlugin(extensionPoints.BPMN_MODELER, myBpmnModelerPlugin);
}
